# Agent layer (labmate/): runs inside the Hermes sandbox and on the host.
pandas>=2.0

# Ingestion / RAG lane (labmate_rag/): host only. Install PyTorch's CPU build
# first so the GPU stays free for vLLM:
#   python3 -m venv .venv && . .venv/bin/activate
#   pip install torch --index-url https://download.pytorch.org/whl/cpu
#   pip install -r requirements.txt
pymupdf>=1.26
sentence-transformers>=3.0
pymongo>=4.9
pytest>=8
